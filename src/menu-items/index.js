// project import
import call from './calls';
import driver from './driver';
import dispatcher from './dispatcher';
import dashboard from './dashboard';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, call, driver, dispatcher]
};

export default menuItems;
